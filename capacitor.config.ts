import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo_imagenes_firestore_21_22',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
